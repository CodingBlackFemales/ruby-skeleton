#!/usr/bin/env ruby

# Allows you to require any file in lib
$: << File.join(File.dirname(__FILE__), '..', 'lib')

# Require files here to run them in the terminal

require 'greeting'

greeting = Greeting.new
puts greeting.say_hello("everyone")
